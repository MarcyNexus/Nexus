/**
 * Placeholder for generated Supabase database types.
 *
 * Once you have a real schema, generate this with the Supabase CLI:
 *   supabase gen types typescript --project-id <your-project-id> > packages/shared/src/database.types.ts
 * then replace this file's export with:
 *   export type { Database } from './database.types';
 */
export type Database = Record<string, unknown>;
